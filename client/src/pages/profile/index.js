import React, { Component } from 'react';
import './main.css';

// TODO: Fix -> cannot destroy 'userid' cookie

class Hero extends Component {
    render() {
        return(
            null
        );
    }
}

export default Hero;
